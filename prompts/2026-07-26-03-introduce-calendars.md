I want to introduce now a concept of calendars. A calendar contains meetings. Every meeting must be part of a calendar. Calendars have priorities denoted by a floating point number. Migrate the existing meetings to a default calendar. Update the database schema. Update the create-meeting endpoints to always accept a calendar name - calendars are identified by unique names like usernames. Once created, a meeting cannot be moved from one calendar to another. Create endpoints for creating and fetching calendars. Calendars should not be per-user. They should exist globally. There should be only one default calendar always in the system with priority 1.0. Any existing user can create a calendar.
