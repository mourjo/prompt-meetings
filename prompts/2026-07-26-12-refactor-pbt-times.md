In the property based test - Use a constant base-time instead of hard coded local date time buried inside. Chnage the meeting creation action to create meeting from base-time + offset to base-time + offset + duration. In the action description, only show the meeting hour/minute so that it is easier to read. Update only the property based test file - Do not update anything else in the source files.
